# URL Redirects — Green & Colours
# Format: /from  /to  status_code

# /rreth-nesh dhe /about NUK ridrejtohen manualisht sepse
# Cloudflare Pages "Clean URLs" i menaxhon automatikisht.
# Redirectet manuale + Clean URLs = redirect loop → ERR_TOO_MANY_REDIRECTS

/about          /rreth-nesh.html   301
